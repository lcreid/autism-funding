<%= form_row do %>
  <%= form_field(f, :child_first_name, 2) %>
  <%= form_field(f, :child_middle_name, 2) %>
  <%= form_field(f, :child_last_name, 2) %>
  <%= form_field(f, :child_dob, 2) %>
  <%= form_field(f, :child_in_care_of_ministry, 2) %>
  <div class="col-md-1">
    <%= link_to my_profile_edit_path(@cf0925.funded_person.user) do %>
      <span class="glyphicon glyphicon-pencil" aria-hidden="true"></span>
      <span class="sr-only">Edit Child Info</span>
    <% end %>
  </div>
<% end %>
